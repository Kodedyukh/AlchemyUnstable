import InteractionArea from 'InteractionArea';

cc.Class({
    extends: InteractionArea,

    properties: {
    },

    // LIFE-CYCLE CALLBACKS:

    // onLoad () {},

    start () {
    },

    // update (dt) {},

    interact(initiator) {
    }
});
